{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "Xalendar Official API docs"
  },
  "host": "localhost:3000",
  "tags": [
    {
      "name": "User"
    }
  ],

  "definitions": {
    "User": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "number"
        },

        "type": {
          "type": "string"
        },

        "full_name": {
          "type": "string"
        }
      }
    },

    "Event": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },

        "color": {
          "type": "string"
        },

        "aud": {
          "type": "string"
        },

        "link": {
          "type": "string"
        },

        "teacher": {
          "type": "string"
        },

        "module_name": {
          "type": "string"
        },

        "theme": {
          "type": "string"
        },

        "start_time": {
          "type": "string"
        },

        "end_time": {
          "type": "string"
        },

        "date": {
          "type": "string"
        },

        "id": {
          "type": "number"
        },

        "meeting_id": {
          "type": "number"
        },

        "user_id": {
          "type": "array"
        }
      }
    },

    "Meeting": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string"
        },

        "start_time": {
          "type": "string"
        },

        "end_time": {
          "type": "string"
        }
      }
    }
  },

  "paths": {
    "/event": {
      "post": {
        "tags": ["Event"],
        "summary": "Post the event to the system.",
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "description": "Event what you want to create",
            "schema": {
              "example": {
                "name": "Практика IT",
                "color": "#232323",
                "aud": "Онлайн",
                "link": "www.discord.com",
                "teacher": "Воробьев А.И.",
                "moduleName": "Мел",
                "theme": "Работа с State, Redux",
                "startTime": "14:15:00",
                "endTime": "15:50:00",
                "date": "2021-05-28",
                "userId": ["269788", "256720"]
              }
            }
          }
        ],

        "produces": ["application/json"],

        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "$/definitions/Event"
            }
          },

          "400": {
            "description": "Failed. Bad post data"
          }
        }
      }
    }
  }
}
